import { ClearControlFieldDirective } from './clear-control-field.directive';

describe('dirClearControlField', () => {
  it('should create an instance', () => {
    const directive = new ClearControlFieldDirective();
    expect(directive).toBeTruthy();
  });
});
