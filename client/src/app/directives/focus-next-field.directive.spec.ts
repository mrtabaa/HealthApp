import { FocusNextFieldDirective } from "./focus-next-field.directive";


describe('FocusNextFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusNextFieldDirective();
    expect(directive).toBeTruthy();
  });
});
