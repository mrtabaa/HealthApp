{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Chrome",
            "request": "launch",
            "type": "pwa-chrome",
            "url": "https://localhost:4200",
            "webRoot": "${workspaceFolder}/client"
        },
        {
            "name": ".NET Core Attach api",
            "type": "coreclr",
            "request": "attach"
        }
    ]
}